This folder is for the packaged local backend runtime used by the Open Source app build.

Expected runtime file name:

- `circle2search-local-backend`

Maintainers should generate it on the Xcode machine before packaging the Open Source app.
See:

- `script/build_local_backend_helper.sh`
- `docs/XCODE_HANDOFF.md`
