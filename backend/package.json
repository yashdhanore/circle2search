{
  "name": "circle2search-backend",
  "private": true,
  "version": "0.1.0",
  "description": "Managed Google Cloud Translation backend for Circle to Search",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/chunking.js && node --check src/googleTranslate.js"
  }
}
